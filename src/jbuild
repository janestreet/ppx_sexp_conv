(library
 ((name ppx_sexp_conv)
  (public_name ppx_sexp_conv)
  (kind ppx_type_conv_plugin)
  (libraries (ppx_core ppx_type_conv ppx_driver ppx_sexp_conv_expander))
  (preprocess no_preprocessing)
  (js_of_ocaml ())
  ))

(jbuild_version jane_street)
